{
  "_README": [
    "Example Evergreen config. Copy to evergreen.config.json and edit.",
    "Shared by the CLI and the engine so their behaviour cannot drift.",
    "",
    "⚠️  GUINEA-PIGS B AND C ARE THE NATURAL-DECAY SUBJECTS for W3-D18-02b/02c.",
    "An early bump destroys weeks of ageing that cannot be recovered inside the",
    "sprint. They MAY sit in 'contracts' once calibrated — the engine correctly",
    "does nothing until the crossing — but only via the verified add / dry-run /",
    "confirm-no-action / go-live procedure. The danger is a mismatched threshold,",
    "not the config entry. Never add either one casually. See docs/SETUP.md",
    "§ Putting B and C into the engine config, and scripts/deploy-guinea-pig.sh.",
    "",
    "Shape checked against EvergreenConfig in packages/shared-types. Underscore-prefixed fields are documentation only; ignored by the future loader."
  ],
  "network": {
    "rpcUrl": "https://soroban-testnet.stellar.org",
    "networkPassphrase": "Test SDF Network ; September 2015"
  },
  "defaults": {
    "bumpWhenRemainingLedgersBelow": 17280,
    "extendToLedgers": 518400
  },
  "contracts": [
    {
      "_note": "Guinea-pig A — the working subject. Safe for the engine to watch.",
      "id": "REPLACE_WITH_GUINEA_PIG_A_ID",
      "label": "guinea-pig-A",
      "payer": "bot-testnet"
    }
  ],
  "_doNotWatch": [
    {
      "_warning": "GUINEA-PIG B — move into 'contracts' only via the verified procedure in docs/SETUP.md.",
      "_why": "It is deliberately ageing so it EXPIRES ~2026-09-21 12:00 UTC (alert threshold ~2026-09-20). DECIDED 2026-09-14 (W3-D18-02b): the engine must NOT save it. This _why previously said the opposite — 'so the engine can save it unattended' — which contradicted the list it sits in and read as an instruction to move B into 'contracts'. Doing that destroys the only unrepeatable evidence in the sprint. Bumping it early destroys the grant's strongest evidence. Calibrated at bumpWhenRemainingLedgersBelow 17280, the engine declines to act until the crossing — so verify that threshold in dry-run before it ever runs live against B.",
      "id": "REPLACE_WITH_GUINEA_PIG_B_ID",
      "label": "guinea-pig-B-natural-decay"
    }
  ],
  "payers": {
    "bot-testnet": {
      "_note": "The engine's own account. Holds only enough XLM to pay extendTTL fees; it has no authority over any contract and needs none, because extendTTL is permissionless. Secret lives in .env, never here.",
      "signer": "ed25519",
      "secretEnvVar": "EVERGREEN_SIGNER_SECRET"
    }
  },
  "notifications": {
    "channel": "email",
    "toEnvVar": "EVERGREEN_ALERT_TO"
  },
  "mode": "dry-run"
}
