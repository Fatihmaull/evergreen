# W3 evidence and task-registry inventory — Sep17

No new chain observation or transaction. `audit.json` records checksum verification
of seven selected W3 bundles and explicit Git snapshot task-ID sets/diffs.
`sep5-count-history.json` locates the historical115-checkbox snapshot instead of
assuming it was the last snapshot of that date. `integrity-repair.json` records the
one failed README checksum, its provenance and restoration against the unchanged
original manifest. No raw RPC, transaction, screenshot or original checksum changed.

Counts are registered checkbox rows, including Done/Dropped, not equally-sized work
units. W1-D4-09 moved out of checkbox rows into a standing obligation; it was not
removed as work. Historical Git snapshots are not independently verified timestamps
of the Notion import. No inference from task counts to consumed slack days is made.
