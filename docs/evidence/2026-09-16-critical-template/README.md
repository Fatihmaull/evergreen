# D17-03 approaching-critical template delivery

One real EmailChannel send on Sep16, after confirming success/failure template
receipts already existed. The source is a real **historical** pre-extension scan
of the disposable contract used in D15-02b, not current chain state. No transaction
or protected-subject action was attempted.

`intent.json` retains the entry/threshold and rendered approachingCritical template,
with an E2E TEST subject and historical explanation. `provider-response.json` is the
unedited provider response; `receipt.json` preserves API acceptance without claiming
inbox placement. `inbox-confirmation.json` separately records Rakha's "masuk inbox".
`verification.json` records an exact re-render and matching provider/receipt/inbox ID.
No success or failure email was sent again just to complete this checklist.

This supplies the missing template-delivery evidence for D17-03. Fatih's task
ownership and final acceptance are unchanged. Check SHA256SUMS before consuming.
